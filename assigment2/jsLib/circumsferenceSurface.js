let cRadius = 4;
let pi = Math.PI

let cArea = pi*(cRadius^2);
let cCircumsference = 2*(pi*cRadius)

console.log("The circumference and surface area of a circle whose radius is 4 are: Circumference = " + cCircumsference + " and Area = " + cArea);